const scores = ['Programming', 5, 'Software Design', 4];

// this is equivalent to:
const scores2: (number | string)[] = ['Programming', 5, 'Software Design', 4];

console.log({
    scores,
    scores2
});

